// <-------------------[sWitCHcAsE]---------------------->
#include<cstdio>
#include<iostream>
#include<algorithm>
#include<cstdlib>
#include<cmath>
#include<cassert>
#include<vector>
#include<map>
#include<cstring>
#include<cassert>
#include<queue>

#define FOR(i,n) for(i=0;i<n;i++)
#define FORS(i,a,n) for(i=a;i<n;i++)
#define ERR(x) cerr<<#x<<" "<<x<<endl
#define pb push_back
using namespace std;
typedef vector<int> VI;
typedef long long ll;
typedef long double ld;
 
int g_init=0;
int g_un;
static int gs_in=1;
static int gs_un;

int fun(double M) {
	return 1;
	
}
int fun(int M) {
	static int v=1,dd=1;
	
	return 1;
} 

int fun(int a,double b) {
	return 1;
}


int funct(const void *a,const void*b) {
return 1;
} 
int main(int argc,char** args)
{
	printf("");
	cout<<endl;
	int arr[]={1,2,3};
	do{
	}while(next_permutation(arr,arr+3));
	qsort(arr,3,sizeof (int),funct);
	queue<int> q;
	map<int,string> mp;
	assert(1==1);
	double a=sqrt(100);
	
 	
}
