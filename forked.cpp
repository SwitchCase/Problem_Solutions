// <-------------------[sWitCHcAsE]---------------------->
#include<cstdio>
#include<iostream>
#include<algorithm>
#include<cstdlib>
#include<cmath>
#include<cassert>
#include<vector>
#include<map>
#include<cstring>
#include<cassert>
#include<queue>

#include<unistd.h>

#define FOR(i,n) for(i=0;i<n;i++)
#define FORS(i,a,n) for(i=a;i<n;i++)
#define ERR(x) cerr<<#x<<" "<<x<<endl
#define pb push_back
using namespace std;
typedef vector<int> VI;
typedef long long ll;
typedef long double ld;
 
int main(int argc,char** args)
{
  int i,j,k;
  char *cp=	(char*)malloc(sizeof(char)*10);
  cout<<"HAHAHHA"<<endl;	
  	cp="ASHISH";
 	if(fork()!=0)
 	{	
 		//cp="ASDASD";	
 		sleep(1);
 		cout<<cp<<endl;
 		for(i=0;i<(1<<29);i++){cerr<<i<<endl;}
 	}
 	else
 	{
 		cp="BLAAAAH";
 		cout<<cp<<endl;
 		sleep(10);
 		for(i=0;i<(1<<29);i++){cerr<<i<<endl;}
 	}
 	return 0;
 	
}
